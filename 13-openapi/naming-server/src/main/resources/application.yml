server:
  port: 8761

spring:
  application:
    name: naming-server

eureka:
  renewalPercentThreshold: 0.85
  client:
    register-with-eureka: false # auto registra��o desabilitada
    fetch-registry: false
    service-url:
      defaultZone: http://naming-server:8761/eureka